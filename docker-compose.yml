version: '3.8'
services:
  front:
    build: 
      context: .
      target: client
    ports:
      - 8080:8080
    command: npm run serve
  back:
    build:
      context: .
      target: server
    depends_on:
      - db
  db:
    image: mariadb
    environment:
      - MARIADB_ROOT_PASSWORD=root
    ports:
      - 3306:3306